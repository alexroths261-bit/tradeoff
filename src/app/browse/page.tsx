import BrowseClient from '@/components/BrowseClient';

export const metadata = {
  title: 'Browse Coupons — TradeOff',
  description: 'Filter and find the best discount coupons across top Indian brands.',
};

export default function BrowsePage() {
  return <BrowseClient />;
}
